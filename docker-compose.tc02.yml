# Tech Challenge Fase 02 — use a stack integrada (Fase 5)
#
# Subir tudo na plataforma (API + Airflow + worker + MLflow):
#   docker compose up -d --build
#
# Treino recomendação: ml_training_dispatch ou POST worker:8010/train
# MLflow UI: http://localhost:5000
#
# Este ficheiro mantém-se só como atalho de documentação — não define rede isolada.

services: {}
