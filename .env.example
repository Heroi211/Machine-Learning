ENVIRONMENT=prd

# Espelho de .env_example — manter sincronizado (cp .env_example .env)
PATH_DATA=src/data/
PATH_DATA_PREPROCESSED=src/data/pre_processed/
PATH_MODEL=src/artifacts/models/
PATH_GRAPHS=src/graphs/
# Sufixo combinado com PATH_DATA → snapshots ficam em src/data/old/ (alinhado à tua pasta ``old``)
PATH_LOGS=old/
MLFLOW_TRACKING_URI=http://localhost:5000
MLFLOW_ARTIFACT_ROOT=src/artifacts/mlruns
PATH_MAINTENANCE_REPORTS=src/artifacts/reports

DEBUG=TRUE

TEST_SIZE=0.2
RANDOM_STATE=42

#CONFIG
PROJECT_NAME='Atlas-Pipeline'
PROJECT_VERSION='/v1'

#JWT
SECRET='aCTsXyVjkJQPXJF53CLVyc5KlDxWcmRzZQFFOvawGfs'

ALGORITHM='HS256'
ACCESS_TOKEN_EXPIRE_MINUTES=30
TIMEZONE='America/Sao_Paulo'

#DATABASE TIP
DATABASE_USER='gabriel_drumond'
DATABASE_PASS='280387'
DATABASE_SERVER='localhost'
DATABASE_PORT='5432'
DATABASE_NAME='processing'
AIRFLOW_DATABASE_NAME=airflow
MLFLOW_DATABASE_NAME=mlflow

LOG_HTTP_REQUESTS_FILE=true
PATH_API_REQUEST_LOGS=logs/api_requests
LOG_HTTP_REQUESTS_MAX_BYTES=5242880
LOG_HTTP_REQUESTS_BACKUP_COUNT=5

ACC_TARGET=0.3
TIME_LIMIT_MINUTES=90
AIRFLOW_UID=1000
PGADMIN_EMAIL=gabriel.drumond@cod3bit.com.br
PGADMIN_PASSWORD=280387
# ↑ Só login do contentor pgAdmin (http://localhost:5050). Ignorado por core.configs Settings.

AIRFLOW_BASE_URL=http://airflow-webserver:8080
AIRFLOW_USER=airflow
AIRFLOW_PASSWORD=airflow
ML_SHARED_PATH=ml_data/uploads

# Raiz do projecto ML (Docker: /opt/airflow/ml_project ou /var/www/ml_shared; local: repo root)
ML_PROJECT_ROOT=

# Airflow → worker recomendação (Docker: http://worker_recommendation:8010; vazio = in-process)
WORKER_RECOMMENDATION_URL=

USE_MLP_FOR_PREDICTION=TRUE